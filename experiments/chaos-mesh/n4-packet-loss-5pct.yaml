apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: n4-packet-loss-5pct
  namespace: social-network
spec:
  action: loss
  mode: all
  selector:
    namespaces:
      - social-network
    labelSelectors:
      app: home-timeline-service
  loss:
    loss: "5"
    correlation: "25"
  duration: "120s"
