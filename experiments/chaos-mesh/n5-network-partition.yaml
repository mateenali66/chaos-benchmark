apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: n5-network-partition
  namespace: social-network
spec:
  action: partition
  mode: all
  selector:
    namespaces:
      - social-network
    labelSelectors:
      app: media-service
  direction: both
  duration: "60s"
