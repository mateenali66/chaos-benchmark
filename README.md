# Chaos Engineering Benchmark

Reproducibility package for the paper:

> **Machine learning for chaos engineering: a taxonomy and empirical benchmark of fault injection tools on microservices**
>
> Mateen Ali Anjum, Phono Technologies Inc., Kitchener, ON, Canada
>
> Submitted to *Software: Practice and Experience* (Wiley), 2026

This repository contains the infrastructure code, experiment definitions, orchestration scripts, raw data, and statistical analysis used to conduct an empirical comparison of **Chaos Mesh 2.8.1** and **LitmusChaos 3.26.0** on a [DeathStarBench](https://github.com/delimitrou/DeathStarBench) Social Network microservices testbed running on AWS EKS.

## Key Results

The original run (120 experiments: 12 fault scenarios x 2 tools x 5 repetitions) was executed on a single 3-node SPOT EKS cluster with a mixed instance pool. A peer reviewer flagged that mixed pool (m5.xlarge/m5a.xlarge/m4.xlarge) as an uncontrolled confound, so the rerun uses a single instance type on ON_DEMAND capacity, run concurrently across three identical clusters (see Infrastructure below). Summary findings from the original run:

| Metric | Chaos Mesh | LitmusChaos | Difference |
|--------|------------|-------------|------------|
| Mean throughput (rps) | 32.9 +/- 4.6 | 19.5 +/- 1.4 | +68.7% |
| p99 latency (ms) | 384.4 | 419.3 | -8.3% (lower is better) |
| Mean error rate (%) | 0.91 | 1.45 | -37.0% (lower is better) |
| Large effect sizes (Cliff's d > 0.474) | 8/12 scenarios | -- | -- |

## Experimental Setup

### Infrastructure

- **Cluster**: AWS EKS 1.31, `ca-central-1`, 3x **ON_DEMAND** `m5.xlarge` (single instance type -- controls for the instance-class/SPOT-interruption confound flagged in review of the original `us-east-1` mixed-SPOT run)
- **Three identical clusters run concurrently**: `is-chaos-bench-a`, `is-chaos-bench-b` (the paired Chaos Mesh vs. LitmusChaos comparison, doubled for throughput/replication), and `is-chaos-ml` (reserved for the ML/LLM fault-selection rework track). Each is a separate Terraform workspace (`terraform/envs/{bench-a,bench-b,ml}.tfvars`) with a non-overlapping `/16` VPC CIDR (`10.10.0.0/16`, `10.20.0.0/16`, `10.30.0.0/16`) in the same account/region. All resource names are prefixed `is-chaos-` and tagged `Project=paper4-chaos-benchmark`, `ManagedBy=terraform`.
- **Shared artifacts bucket**: a single `is-chaos-artifacts-<account_id>` S3 bucket, owned by the `bench-a` workspace, with `scripts/export-to-s3.sh` writing under a `<cluster-name>/data/<timestamp>/` key prefix per cluster so all three can share it safely.
- **Watchdogs**: `scripts/watchdogs/infra-watchdog.py` (node readiness, stuck-Pending pods, kube-system OOMKills/evictions, PVC binding failures) and `scripts/watchdogs/experiment-watchdog.py` (campaign stall detection, FAILED runs, dead runner process) poll every 60s per cluster and write JSONL status + an `.ALERT` sentinel file under `data/watchdogs/<env>/`. Launch both for a cluster with `./scripts/watchdogs/watch-all.sh <bench-a|bench-b|ml>`.
- **Application**: DeathStarBench Social Network (27 microservices, 962 users in social graph)
- **Monitoring**: Prometheus (kube-prometheus-stack) + Grafana + Jaeger

### Fault Scenarios

| ID | Scenario | Category | Target Service |
|----|----------|----------|----------------|
| P1 | Pod Kill | Pod/Container | compose-post-service |
| P2 | Container Kill | Pod/Container | nginx-thrift |
| P3 | Pod Failure | Pod/Container | user-service |
| N1 | Network Latency 50ms | Network | social-graph-service |
| N2 | Network Latency 100ms | Network | post-storage-service |
| N3 | Network Latency 300ms | Network | user-timeline-service |
| N4 | Packet Loss 5% | Network | home-timeline-service |
| N5 | Network Partition | Network | media-service |
| R1 | CPU Stress 80% | Resource | compose-post-service |
| R2 | Memory Pressure 80% | Resource | post-storage-service |
| A1 | HTTP Abort 503 | Application | nginx-thrift |
| A2 | gRPC Unavailable | Application | user-service |

### Protocol

Each experiment follows a four-phase protocol:

1. **Baseline** (5 min) -- steady-state metrics under load (wrk2 at 200 rps)
2. **Fault injection** (2 min) -- apply chaos experiment via `kubectl`
3. **Recovery** (1 min) -- remove fault, measure time to recover
4. **Cooldown** (1 min) -- ensure stability before next run

Load is generated by [wrk2](https://github.com/giltene/wrk2) running a mixed workload: 40% compose post, 30% read home timeline, 20% read user timeline, 10% follow user.

## Repository Structure

```
chaos-benchmark/
├── analysis/                    # Statistical analysis and visualization
│   ├── analyze.py               # Main analysis script (Wilcoxon, Cliff's delta, Bonferroni)
│   ├── figures.py               # Figure generation (matplotlib)
│   ├── tables.py                # LaTeX table generation
│   ├── architecture_diagram.py  # Architecture diagram (Python diagrams library)
│   ├── figures/                 # Generated figures (PDF + PNG)
│   └── results/                 # CSV output (summary stats, statistical tests, etc.)
├── data/                        # Raw experiment data (120 JSON files, ~48 MB)
│   ├── chaos-mesh/{scenario}/run-{1..5}.json
│   └── litmus/{scenario}/run-{1..5}.json
├── experiments/
│   ├── scenarios.yaml           # Master scenario definitions with hypotheses
│   ├── chaos-mesh/              # 12 Chaos Mesh YAML manifests
│   └── litmus/                  # 12 LitmusChaos YAML manifests
├── helm/                        # Helm values for tool installation
│   ├── chaos-mesh-values.yaml
│   ├── litmus-values.yaml
│   └── prometheus-values.yaml
├── load-generator/
│   ├── Dockerfile               # wrk2 Docker image
│   ├── mixed-workload.lua       # Mixed workload script (40/30/20/10)
│   └── wrk2-job.yaml.tpl       # Kubernetes Job template
├── manifests/                   # Kubernetes manifests (namespaces, RBAC, Jaeger)
├── scripts/
│   ├── setup.sh                 # Cluster bootstrap (Helm installs, kubeconfig)
│   ├── teardown.sh              # Cluster teardown (terraform destroy)
│   ├── deploy-dsb.sh            # Deploy DeathStarBench Social Network
│   ├── post-deploy.sh           # RBAC, CRDs, social graph initialization
│   ├── run-experiment.py        # Core experiment orchestrator (~680 lines)
│   ├── run-experiment.sh        # Thin bash wrapper
│   ├── run-all-experiments.sh   # Batch runner for all 120 experiments (resume support)
│   ├── smoke-test.sh            # End-to-end validation (13 checks)
│   └── export-to-s3.sh          # Data backup to S3
├── terraform/                   # EKS cluster infrastructure (VPC, EKS, addons)
│   ├── main.tf
│   ├── variables.tf
│   ├── envs/                    # Per-cluster tfvars (bench-a.tfvars, bench-b.tfvars, ml.tfvars)
│   └── modules/{vpc,eks,eks-addons}/
├── scripts/watchdogs/           # Long-running monitors for the campaign
│   ├── infra-watchdog.py        # Node/pod/PVC health per cluster
│   ├── experiment-watchdog.py   # Campaign stall / failure / runner-liveness
│   └── watch-all.sh             # Launches both, nohup, per cluster
└── DeathStarBench/              # Git submodule
```

## Reproducing the Experiments

### Prerequisites

- AWS account with permissions for EKS, VPC, EC2, S3, ECR
- AWS CLI configured with a named profile
- Terraform >= 1.5
- kubectl
- Helm 3
- Python 3.10+
- Docker (for building the wrk2 image)

### 1. Provision infrastructure

Each of the three clusters (`bench-a`, `bench-b`, `ml`) is its own Terraform workspace with its own tfvars file under `terraform/envs/`. Fill in `cluster_admin_arns` in each file before applying.

```bash
cd terraform
terraform init

# Repeat for bench-a, bench-b, ml (bench-a should be applied first: it owns
# the shared S3 artifacts bucket the other two write into)
terraform workspace new bench-a   # or: terraform workspace select bench-a
terraform apply -var-file=envs/bench-a.tfvars

# Bootstrap a cluster (Helm charts, kubeconfig, namespaces) -- takes the same env name
cd ..
./scripts/setup.sh bench-a
```

### 2. Deploy the application and chaos tools

```bash
# Deploy DeathStarBench, Litmus RBAC/CRDs, initialize social graph
./scripts/post-deploy.sh

# Validate everything works (13 checks)
./scripts/smoke-test.sh
```

### 3. Build the load generator

```bash
./scripts/build-wrk2-image.sh
```

### 4. Run experiments

```bash
# Single experiment (~9 min)
./scripts/run-experiment.sh --tool chaos-mesh --scenario p1 --run 1

# All 120 experiments (~22 hours, with resume support)
./scripts/run-all-experiments.sh
```

### 5. Analyze results

```bash
cd analysis
python3 analyze.py    # Summary stats, statistical tests
python3 figures.py    # Generate all figures
python3 tables.py     # Generate LaTeX tables
```

### 6. Tear down

```bash
./scripts/teardown.sh bench-a
terraform -chdir=terraform destroy -var-file=envs/bench-a.tfvars

# Repeat per cluster; tear down bench-a LAST since it owns the shared
# is-chaos-artifacts bucket that bench-b and ml write into.
```

### Estimated Cost

Running the full experiment set takes approximately 22 hours of cluster time, per cluster. The rerun runs three identical clusters concurrently, and ON_DEMAND capacity costs more than SPOT (traded off deliberately, to remove the SPOT-interruption confound the original run had).

| Resource | Cost (per cluster/day) |
|----------|------|
| EKS control plane | ~$2.40/day |
| 3x ON_DEMAND m5.xlarge | ~$8.30/day |
| EBS volumes | ~$0.30/day |
| **Subtotal per cluster** | **~$11.00/day** |
| **Total across 3 concurrent clusters** | **~$33.00/day** |

## Data Format

Each experiment produces a JSON file in `data/{tool}/{scenario}/run-{N}.json` containing:

- **wrk2 output**: throughput (rps), latency percentiles (p50, p95, p99), error counts, timeouts
- **Prometheus metrics**: per-phase (baseline, fault, recovery) CPU usage, memory usage, pod restarts, network I/O
- **Metadata**: tool, scenario, run number, timestamps, protocol timing

## Statistical Methods

- **Wilcoxon signed-rank test** (non-parametric, paired) for tool comparison
- **Cliff's delta** for effect size (small/medium/large)
- **Bonferroni correction** for multiple comparisons (36 tests)
- **95% confidence intervals** computed via t-distribution

## Citation

If you use this benchmark in your research, please cite:

```bibtex
@article{anjum2026chaos,
  title={Machine learning for chaos engineering: a taxonomy and empirical
         benchmark of fault injection tools on microservices},
  author={Anjum, Mateen Ali},
  journal={Software: Practice and Experience},
  year={2026},
  note={Under review}
}
```

## License

MIT License. See [LICENSE](LICENSE) for details.
